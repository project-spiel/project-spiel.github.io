<!DOCTYPE html>
<html>
  {% include head.liquid %}
<body>
  {% include header.liquid %}
  <main>
    <h1>{{ page.title }}</h1>
    {% comment %} {{ page | debug }} {% endcomment %}
    {{ content }}
  </main>
  {% include footer.liquid %}
</body>

</html>